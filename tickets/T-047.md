# T-047 — Implement Stripe Checkout Frontend UI

**Source finding:** AUDIT_REPORT.md §4 | Severity: P1 | Class: MISSING
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 2h | **Sequence:** after T-046

## WHAT
Update the `FeasibilityDossierModal` and related UI to call the actual `/api/checkout/dossier` endpoint, redirecting the user to Stripe Checkout on success.

## WHY
With the backend implemented, the frontend needs to connect to the new endpoint to enable actual purchases and drive revenue.

## SCOPE — FILES
IN:  `src/client/components/FeasibilityDossierModal.tsx`, `src/client/components/NotEnoughData.tsx`
OUT: `src/server/*` (Backend is already handled in T-046)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: Clicking "Purchase Dossier" sends a POST request to `/api/checkout/dossier`.
- [ ] AC2: On successful response containing `url`, `window.location.href` is updated to the Stripe Checkout URL.
- [ ] AC3: If the request fails, an appropriate error state is shown in the UI.

## TEST PLAN
Run the frontend locally, click the purchase button, and verify the network tab shows the correct POST request and redirection.

## CONSTRAINTS
- Follow existing Tailwind CSS styling and Apple HIG principles.
- Use standard `fetch` API.

## ROLLBACK
Revert commit on the frontend components.

## BLOCKED PROTOCOL
If the backend endpoint is not functioning or returns 503, STOP and report.
