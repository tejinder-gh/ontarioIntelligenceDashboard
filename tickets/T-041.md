# T-041 — Fail closed dossier checkout

**Source finding:** AUDIT_REPORT.md §CTO-01 | Severity: P0 | Class: BROKEN
**Assigned tier:** SENIOR
**Effort estimate:** 3h | **Sequence:** independent

## WHAT

Disable public paid checkout until a real payment provider and webhook are approved. Prevent confirmed orders, fake Stripe URLs and implied tax collection.

## WHY

Current code calls a fake checkout successful and stores `CONFIRMED` before payment.

## SCOPE — FILES

IN: `src/server/routes.ts`, `tests/dossier.test.ts`
OUT: `src/db/schema.sql` (no payment schema design), `src/client/**`, Stripe configuration and external-provider setup.

## ACCEPTANCE CRITERIA (testable, binary)

- [ ] AC1: `POST /api/checkout/dossier` returns a generic unavailable response and writes no `dossier_orders` row.
- [ ] AC2: No response includes fabricated Stripe data or a `CONFIRMED` order.
- [ ] AC3: Existing dossier-preview GET behavior is unchanged.
- [ ] AC4: Tests prove checkout creates no order; all verification commands pass.

## TEST PLAN

Run `bun run test`, `bunx tsc --noEmit`, and `bun run build`; add database-backed no-write test.

## CONSTRAINTS

- No Stripe SDK, payment call, tax logic or webhook.
- Generic, non-sensitive unavailable response only.

## ROLLBACK

Revert the ticket commit; do not restore fake payment behavior.

## BLOCKED PROTOCOL

If tests expect confirmed orders, update them to assert fail-closed behavior.
