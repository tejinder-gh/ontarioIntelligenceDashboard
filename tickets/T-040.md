# T-040 — Remove alert operator UI

**Source finding:** AUDIT_REPORT.md §CTO-02 and §DES-01 | Severity: P0 | Class: BROKEN
**Assigned tier:** INTERMEDIATE
**Effort estimate:** 2h | **Sequence:** after T-039

## WHAT

Remove subscriber-facing controls that read global pending notifications or invoke evaluation. Present alert subscriptions as unavailable until a consented delivery service is specified.

## WHY

The current UI exposes internal operations and indicates a delivery service exists when it cannot safely deliver messages.

## SCOPE — FILES

IN: `src/client/components/AlertSubscriptionModal.tsx`, `src/client/App.tsx`, `tests/alerts-diff.test.ts`
OUT: `src/server/**`, `src/alerts/**`, all dossier files.

## ACCEPTANCE CRITERIA (testable, binary)

- [ ] AC1: Client makes no request to pending, evaluate or deliver alert endpoints.
- [ ] AC2: No UI text claims alerts will be delivered or exposes a run-evaluator action.
- [ ] AC3: Modal remains keyboard-dismissible and does not persist subscriber email in browser storage.
- [ ] AC4: `bun run test`, `bunx tsc --noEmit`, and `bun run build` pass.

## TEST PLAN

Add focused component/source assertions, then execute the three verification commands.

## CONSTRAINTS

- No new dependencies, fake channel, data collection or email sender.

## ROLLBACK

Revert the ticket commit; retain T-039 server lockdown.

## BLOCKED PROTOCOL

If live alerts are required, STOP and request consent/delivery requirements.
