# T-036 — Parallelize sequential database queries in computeEmpiricalCoverage

**Source finding:** AUDIT_REPORT.md §CTO-17 | Severity: P2 | Class: SUBOPTIMAL  
**Assigned tier:** INTERMEDIATE  
**Effort estimate:** 2h | **Sequence:** 4

## WHAT
In `src/server/routes.ts`, refactor `computeEmpiricalCoverage` from 11 sequential `await sql` queries into concurrent `Promise.all` execution across independent metric coverage counts.

## WHY
Reduces database round-trip latency on `/api/geographies/:id/profile` and `/api/geographies/:id/coverage` from ~120ms to under 15ms.

## SCOPE — FILES
IN:
- `src/server/routes.ts`

OUT:
- `src/server/app.ts`

## ACCEPTANCE CRITERIA
- [ ] AC1: `computeEmpiricalCoverage` executes independent coverage counts concurrently using `Promise.all`.
- [ ] AC2: All returned coverage metrics match previous values exactly.
- [ ] AC3: Full test suite (`npm test`) passes with 0 failures.

## TEST PLAN
```bash
npm test tests/coverage.test.ts
npm test
```
