# T-048 — Implement email dispatcher service via Resend

**Source finding:** AUDIT_REPORT.md §Phase 5 & 6 | Severity: P0 | Class: MISSING
**Assigned tier:** SENIOR
**Effort estimate:** 2h | **Sequence:** independent

## WHAT
Implement a new robust email dispatcher service in `src/alerts/email-dispatcher.ts` using the Resend SDK. The service should expose a function `sendDossierEmail(email, cityId, categoryId)` that sends a cleanly formatted HTML email confirming the purchase and containing the dossier access link or attached PDF.

## WHY
Currently, there is no email infrastructure, which blocks product fulfillment (100% chargeback risk) and breaks the core monetization loop.

## SCOPE — FILES
IN: 
- `src/alerts/email-dispatcher.ts` (NEW)
- `package.json`

OUT: 
- `src/server/stripe-webhook.ts` (handled in T-049)
- `src/alerts/watch-evaluator.ts` (Do not hook up watches yet)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: `resend` package is added to dependencies in `package.json`.
- [ ] AC2: `src/alerts/email-dispatcher.ts` exports `sendDossierEmail` which accepts `to`, `cityId`, and `categoryId`.
- [ ] AC3: Service uses `process.env.RESEND_API_KEY` and handles errors gracefully (logging and returning `false` on failure).
- [ ] AC4: A basic unit test in `tests/email.test.ts` exists to mock Resend and verify the function calls the Resend API with the correct payload.

## TEST PLAN
```bash
bun run test tests/email.test.ts
```
Expected output: Test passes indicating the mocked resend function was called with proper HTML payload.

## CONSTRAINTS
- Follow existing patterns in `src/alerts/watch-evaluator.ts` for service abstraction.
- Allowed dependency: `resend` @ latest.
- TypeScript strict, no `any`.

## ROLLBACK
Delete `src/alerts/email-dispatcher.ts` and revert `package.json`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
