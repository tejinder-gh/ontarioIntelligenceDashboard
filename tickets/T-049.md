# T-049 — Fulfill dossier orders via email from Stripe Webhook

**Source finding:** AUDIT_REPORT.md §Phase 5 & 6 | Severity: P0 | Class: BROKEN
**Assigned tier:** SENIOR
**Effort estimate:** 1h | **Sequence:** after T-048

## WHAT
Update `src/server/stripe-webhook.ts` to extract the `customer_email`, `cityId`, and `categoryId` from the `checkout.session.completed` event. Use the newly created `sendDossierEmail` function to send the dossier to the customer, and record the `fulfilled_at` timestamp in the `dossier_orders` table.

## WHY
Currently, users are charged $199 but receive nothing. Calling the email dispatcher on successful payment closes the fulfillment loop.

## SCOPE — FILES
IN: 
- `src/server/stripe-webhook.ts`
- `src/db/schema.sql` (if schema changes are needed to add fulfilled_at)

OUT: 
- `src/alerts/email-dispatcher.ts` (Already built in T-048)

## ACCEPTANCE CRITERIA (testable, binary)
- [ ] AC1: `checkout.session.completed` handler reads `customer_email`, `metadata.cityId`, and `metadata.categoryId`.
- [ ] AC2: `sendDossierEmail` is called with those parameters.
- [ ] AC3: If email succeeds, `dossier_orders.fulfilled_at` is set to `NOW()`.
- [ ] AC4: Modify `src/db/schema.sql` to add a `fulfilled_at TIMESTAMPTZ` column to the `dossier_orders` table.

## TEST PLAN
```bash
bun run test tests/dossier.test.ts
```
Expected output: Tests pass, or write a new test simulating a webhook payload that verifies email dispatcher is called.

## CONSTRAINTS
- Handle webhook idempotency/retries properly.
- TypeScript strict, no `any`.

## ROLLBACK
Revert `src/server/stripe-webhook.ts`.

## BLOCKED PROTOCOL
If any assumption here proves false against the real code: STOP, write what you found vs. what the ticket claimed, status → BLOCKED. Do not improvise around it.
