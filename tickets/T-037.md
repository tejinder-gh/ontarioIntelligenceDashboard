# T-037 — Add Escape key modal dismissal & keyboard accessibility

**Source finding:** AUDIT_REPORT.md §DES-7 | Severity: P2 | Class: SUBOPTIMAL / DESIGN  
**Assigned tier:** INTERMEDIATE  
**Effort estimate:** 1h | **Sequence:** 5

## WHAT
In `src/client/components/FeasibilityDossierModal.tsx` and `src/client/components/AlertSubscriptionModal.tsx`:
1. Add `useEffect` listener for `keydown` events matching `Escape` to close the modal.
2. Ensure proper `role="dialog"` and `aria-modal="true"` attributes are set.

## WHY
Enforces WCAG 2.1 AA keyboard accessibility and improves user experience on desktop/laptop keyboards.

## SCOPE — FILES
IN:
- `src/client/components/FeasibilityDossierModal.tsx`
- `src/client/components/AlertSubscriptionModal.tsx`

## ACCEPTANCE CRITERIA
- [ ] AC1: Pressing the `Escape` key closes `FeasibilityDossierModal` when open.
- [ ] AC2: Pressing the `Escape` key closes `AlertSubscriptionModal` when open.
- [ ] AC3: Event listener is cleanly removed on unmount.
- [ ] AC4: Full test suite (`npm test`) passes with 0 failures.

## TEST PLAN
```bash
npm test
npx tsc --noEmit
```
