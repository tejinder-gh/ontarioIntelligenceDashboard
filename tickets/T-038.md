# T-038 — Fix Alert modal email prefill and wire VC tab deep linking

**Source finding:** AUDIT_REPORT.md §DES-8, DES-9 | Severity: P2 | Class: SUBOPTIMAL  
**Assigned tier:** INTERMEDIATE  
**Effort estimate:** 1h | **Sequence:** 6

## WHAT
1. In `src/client/components/AlertSubscriptionModal.tsx`, remove the hardcoded `'investor@ontario-intelligence.ca'` default fallback so that the email input is empty when no `localStorage` email exists, requiring genuine user input.
2. In `src/client/App.tsx`, add `'venture_capital'` to `isCityTab()` so deep-linking URLs like `/city/:name/venture-capital` are preserved during navigation.

## WHY
Prevents capturing junk placeholder emails and ensures browser forward/back history functions correctly on the venture capital view.

## SCOPE — FILES
IN:
- `src/client/components/AlertSubscriptionModal.tsx`
- `src/client/App.tsx`

## ACCEPTANCE CRITERIA
- [ ] AC1: `AlertSubscriptionModal` starts with an empty email input if `localStorage` has no stored address.
- [ ] AC2: `App.tsx` generates `/city/:name/venture-capital` deep link URLs when viewing VC module with a selected city.
- [ ] AC3: Full test suite (`npm test`) passes with 0 failures.

## TEST PLAN
```bash
npm test
npm run build
```
